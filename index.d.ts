export declare class RicohView {
    constructor(args);

    toggleRotation();

    rotateCamera(x, y);

    setCameraDir(alpha, beta, gamma);

    zoomCamera(val);

    show();

    stopRendering();

}